import { makeStyles, Theme } from "@material-ui/core/styles";

export const useStyles = makeStyles((theme) => ({
  contentItemContainer: {
    border: "1px dotted #dd42f5",
    width: "auto",
    marginTop: "5px",
    padding: "10px",
  },
}));
